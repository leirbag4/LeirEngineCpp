#include "LeirEngine/Components/AudioListener.h"

// The per-frame listener push lives in AudioSyncSystem (Incremento 3): this
// component is now pure data (a marker), no lifecycle.
namespace Leir {
} // namespace Leir