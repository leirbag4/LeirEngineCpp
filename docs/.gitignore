# Herramientas portátiles descargadas por setup_tools.bat — NO se commitean.
tools/
# XML intermedio de Doxygen (Breathe/Exhale lo consumen) — NO se commitea.
sphinx/_xml/
# Árbol de API generado por Exhale (se regenera en cada build) — NO se commitea.
sphinx/api/
# Sitio generado — NO se commitea (se regenera con generate_docs.bat).
site/